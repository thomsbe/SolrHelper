<?xml version="1.0" encoding="UTF-8" ?>
<!--
  Solr 5.5.5 schema for testing purposes
  This schema defines various field types and example fields for testing.
-->
<schema name="testing" version="1.6">
  <!-- Felddefinitionen für verschiedene Datentypen -->
  <fields>
    <!-- Standardfelder -->
    <field name="_version_" type="long" indexed="true" stored="true"/>
    <field name="_root_" type="string" indexed="true" stored="false"/>
    <field name="id" type="string" indexed="true" stored="true" required="true" multiValued="false" />
    
    <!-- Beispiel für ein Textfeld mit Analyse -->
    <field name="title" type="text_general" indexed="true" stored="true" multiValued="false" />
    <field name="content" type="text_general" indexed="true" stored="true" multiValued="false" />
    
    <!-- Beispiel für ein String-Feld (exakte Suche) -->
    <field name="status" type="string" indexed="true" stored="true" multiValued="false" />
    <field name="category" type="string" indexed="true" stored="true" multiValued="true" />
    
    <!-- Beispiel für numerische Felder -->
    <field name="price" type="double" indexed="true" stored="true" multiValued="false" />
    <field name="quantity" type="int" indexed="true" stored="true" multiValued="false" />
    
    <!-- Beispiel für ein Datumsfeld -->
    <field name="created_at" type="date" indexed="true" stored="true" multiValued="false" />
    <field name="updated_at" type="date" indexed="true" stored="true" multiValued="false" />
    
    <!-- Beispiel für ein Boolesches Feld -->
    <field name="is_active" type="boolean" indexed="true" stored="true" multiValued="false" />
    
    <!-- Beispiel für ein mehrwertiges Textfeld -->
    <field name="tags" type="text_general" indexed="true" stored="true" multiValued="true" />
    
    <!-- Kopierfelder für die Volltextsuche -->
    <field name="text" type="text_general" indexed="true" stored="false" multiValued="true"/>
  </fields>

  <!-- Eindeutiger Schlüssel -->
  <uniqueKey>id</uniqueKey>

  <!-- Kopierfelder für die Volltextsuche -->
  <copyField source="title" dest="text"/>
  <copyField source="content" dest="text"/>
  <copyField source="category" dest="text"/>
  <copyField source="tags" dest="text"/>

  <!-- Feldtypen -->
  <types>
    <!-- Feldtypdefinitionen -->
    <fieldType name="string" class="solr.StrField" sortMissingLast="true" />
    
    <!-- Boolean -->
    <fieldType name="boolean" class="solr.BoolField" sortMissingLast="true"/>
    
    <!-- Numerische Typen -->
    <fieldType name="int" class="solr.TrieIntField" precisionStep="0" positionIncrementGap="0"/>
    <fieldType name="long" class="solr.TrieLongField" precisionStep="0" positionIncrementGap="0"/>
    <fieldType name="float" class="solr.TrieFloatField" precisionStep="0" positionIncrementGap="0"/>
    <fieldType name="double" class="solr.TrieDoubleField" precisionStep="0" positionIncrementGap="0"/>
    
    <!-- Datum/Zeit -->
    <fieldType name="date" class="solr.TrieDateField" precisionStep="0" positionIncrementGap="0"/>
    
    <!-- Textfeld mit Analyse -->
    <fieldType name="text_general" class="solr.TextField" positionIncrementGap="100">
      <analyzer type="index">
        <tokenizer class="solr.StandardTokenizerFactory"/>
        <filter class="solr.StopFilterFactory" words="stopwords.txt" ignoreCase="true"/>
        <filter class="solr.SynonymFilterFactory" synonyms="synonyms.txt" ignoreCase="true" expand="true"/>
        <filter class="solr.LowerCaseFilterFactory"/>
      </analyzer>
      <analyzer type="query">
        <tokenizer class="solr.StandardTokenizerFactory"/>
        <filter class="solr.StopFilterFactory" words="stopwords.txt" ignoreCase="true"/>
        <filter class="solr.SynonymFilterFactory" synonyms="synonyms.txt" ignoreCase="true" expand="true"/>
        <filter class="solr.LowerCaseFilterFactory"/>
      </analyzer>
    </fieldType>
    
    <!-- Feldtyp für exakte Suche ohne Analyse -->
    <fieldType name="text_exact" class="solr.TextField" positionIncrementGap="100">
      <analyzer>
        <tokenizer class="solr.KeywordTokenizerFactory"/>
        <filter class="solr.LowerCaseFilterFactory"/>
      </analyzer>
    </fieldType>
  </types>
</schema>
